name: onboarding customer

on:
  # workflow_dispatch: {}
  workflow_run:
    workflows: ["Update customer info"]
    type: 
      - "completed"
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: test
        run: |
          echo "hi"
          for i in 1 2 3 4 5
          do
            echo "Mendax Current number: $i"
          done