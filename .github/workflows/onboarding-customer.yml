name: onboarding customer

on:
  workflow_dispatch: {}

jobs:
  job1:
    uses: zhang35/test-bash-jiaqi/.github/workflows/update-customer-info.yml@v1
  job2:
    runs-on: ubuntu-latest
    needs: job1
    steps:
      - run: echo ${{ needs.job1.outputs.org_name }}